const add = (first, second) => {
    return first + second;
};

const multiply = (first, second) => {
    return first * second;
};

const divideFirstNumberWithSecoundNumber = (first, second) => {
    return first / second;
};

// export default add; // for only one function

export { add, divideFirstNumberWithSecoundNumber, multiply };

